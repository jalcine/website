---
layout: default
title: jackyalciné
---

#home
  %ul.posts
  {% for post in site.posts %}
    %li
      %span= "{{ post.date }}"
      &raquo;
      %a{href: "{{ post.url }}" }= " {{ post.title }}"
  {% endfor %}
